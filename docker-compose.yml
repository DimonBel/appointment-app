version: '3.8'

# YAML Anchors for reusable configurations
x-default-env: &default-env
  POSTGRES_USER: ${POSTGRES_USER}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

x-db-healthcheck: &db-healthcheck
  test: ["CMD-SHELL", "pg_isready -U postgres"]
  interval: 10s
  timeout: 5s
  retries: 5

x-postgres-template: &postgres-template
  image: postgres:16-alpine
  restart: unless-stopped
  environment:
    <<: *default-env
  networks:
    - app-network
  healthcheck: *db-healthcheck

x-common-service-deps: &common-service-deps
  identity-service:
    condition: service_healthy

services:
  # Ollama LLM Service for AI Assistant
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - app-network
    environment:
      - OLLAMA_KEEP_ALIVE=-1
      - OLLAMA_NUM_PARALLEL=2
    command: serve

  # PostgreSQL Databases
  identity-db:
    <<: *postgres-template
    container_name: identity-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${IDENTITY_DB_NAME}
    ports:
      - "5436:5432"
    volumes:
      - identity-db-data:/var/lib/postgresql/data

  appointment-db:
    <<: *postgres-template
    container_name: appointment-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${APPOINTMENT_DB_NAME}
    ports:
      - "5433:5432"
    volumes:
      - appointment-db-data:/var/lib/postgresql/data

  chat-db:
    <<: *postgres-template
    container_name: chat-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${CHAT_DB_NAME}
    ports:
      - "5434:5432"
    volumes:
      - chat-db-data:/var/lib/postgresql/data

  notification-db:
    <<: *postgres-template
    container_name: notification-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${NOTIFICATION_DB_NAME}
    ports:
      - "5435:5432"
    volumes:
      - notification-db-data:/var/lib/postgresql/data

  document-db:
    <<: *postgres-template
    container_name: document-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${DOCUMENT_DB_NAME}
    ports:
      - "5437:5432"
    volumes:
      - document-db-data:/var/lib/postgresql/data

  automation-db:
    <<: *postgres-template
    container_name: automation-db
    environment:
      <<: *default-env
      POSTGRES_DB: ${AUTOMATION_DB_NAME}
    ports:
      - "5438:5432"
    volumes:
      - automation-db-data:/var/lib/postgresql/data

  # MinIO Object Storage
  minio:
    image: minio/minio:latest
    container_name: minio
    restart: unless-stopped
    entrypoint: ["/entrypoint.sh"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
      - ./minio-entrypoint.sh:/entrypoint.sh:ro
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/minio/health/live || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Backend Services
  identity-service:
    container_name: identity-service
    restart: unless-stopped
    build:
      context: ./Identity
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5005
      - ConnectionStrings__DefaultConnection=Host=identity-db;Port=5432;Database=${IDENTITY_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
      - Jwt__AccessTokenExpirationMinutes=${JWT_ACCESS_EXPIRATION}
      - Jwt__RefreshTokenExpirationDays=${JWT_REFRESH_EXPIRATION}
      - NotificationService__BaseUrl=http://notification-service:5003
      - Frontend__PublicBaseUrl=${FRONTEND_PUBLIC_BASE_URL}
      - IdentityService__PublicBaseUrl=${IDENTITY_PUBLIC_BASE_URL}
      - Minio__Endpoint=${MINIO_ENDPOINT}
      - Minio__AccessKey=${MINIO_ROOT_USER}
      - Minio__SecretKey=${MINIO_ROOT_PASSWORD}
      - Minio__Bucket=${MINIO_BUCKET}
      - Minio__UseSsl=${MINIO_USE_SSL}
      - Minio__PublicBaseUrl=${MINIO_PUBLIC_BASE_URL}
      - Smtp__Host=${SMTP_HOST}
      - Smtp__Port=${SMTP_PORT}
      - Smtp__Username=${SMTP_USERNAME}
      - Smtp__Password=${SMTP_PASSWORD}
      - Smtp__FromEmail=${SMTP_FROM_EMAIL}
      - Smtp__FromName=${SMTP_FROM_NAME}
      - Smtp__SecureSocket=${SMTP_SECURE_SOCKET}
      - Smtp__UseSsl=false
      - Smtp__UseStartTls=true
      - SeedAdmin__Email=${SEED_ADMIN_EMAIL}
      - SeedAdmin__Password=${SEED_ADMIN_PASSWORD}
      - SeedAdmin__UserName=${SEED_ADMIN_USERNAME}
      - SeedAdmin__FirstName=${SEED_ADMIN_FIRSTNAME}
      - SeedAdmin__LastName=${SEED_ADMIN_LASTNAME}
      - SeedAdmin__ResetPasswordOnStartup=${SEED_ADMIN_RESET_PASSWORD_ON_STARTUP}
    ports:
      - "5005:5005"
    depends_on:
      identity-db:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5005/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  appointment-service:
    container_name: appointment-service
    restart: unless-stopped
    build:
      context: .
      dockerfile: Appointment/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5001
      - ConnectionStrings__DefaultConnection=Host=appointment-db;Port=5432;Database=${APPOINTMENT_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - IdentityService__BaseUrl=http://identity-service:5005
      - NotificationService__BaseUrl=http://notification-service:5003
      - DocumentService__BaseUrl=http://document-service:5004
      - DocumentService__PublicBaseUrl=${DOCUMENT_PUBLIC_BASE_URL}
      - InternalServiceKey=${INTERNAL_SERVICE_KEY}
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
      - Jwt__ExpirationMinutes=${JWT_ACCESS_EXPIRATION}
    ports:
      - "5001:5001"
    depends_on:
      appointment-db:
        condition: service_healthy
      <<: *common-service-deps
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5001/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  chat-service:
    container_name: chat-service
    restart: unless-stopped
    build:
      context: ./ChatApp
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5002
      - ConnectionStrings__DefaultConnection=Host=chat-db;Port=5432;Database=${CHAT_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - IdentityService__BaseUrl=http://identity-service:5005
      - NotificationService__BaseUrl=http://notification-service:5003
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
    ports:
      - "5002:5002"
    depends_on:
      chat-db:
        condition: service_healthy
      <<: *common-service-deps
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5002/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  notification-service:
    container_name: notification-service
    restart: unless-stopped
    build:
      context: ./Notification
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5003
      - ConnectionStrings__DefaultConnection=Host=notification-db;Port=5432;Database=${NOTIFICATION_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - IdentityService__BaseUrl=http://identity-service:5005
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
      - Smtp__Host=${SMTP_HOST}
      - Smtp__Port=${SMTP_PORT}
      - Smtp__Username=${SMTP_USERNAME}
      - Smtp__Password=${SMTP_PASSWORD}
      - Smtp__FromEmail=${SMTP_FROM_EMAIL}
      - Smtp__FromName=${SMTP_FROM_NAME}
      - Smtp__SecureSocket=${SMTP_SECURE_SOCKET}
      - Smtp__UseSsl=false
      - Smtp__UseStartTls=true
    ports:
      - "5003:5003"
    depends_on:
      notification-db:
        condition: service_healthy
      <<: *common-service-deps
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5003/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  document-service:
    container_name: document-service
    restart: unless-stopped
    build:
      context: ./Document
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5004
      - ConnectionStrings__DefaultConnection=Host=document-db;Port=5432;Database=${DOCUMENT_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - Minio__Bucket=${MINIO_BUCKET_DOCUMENTS}
      - InternalServiceKey=${INTERNAL_SERVICE_KEY}
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
      - Minio__Endpoint=${MINIO_ENDPOINT}
      - Minio__AccessKey=${MINIO_ROOT_USER}
      - Minio__SecretKey=${MINIO_ROOT_PASSWORD}
      - Minio__UseSSL=${MINIO_USE_SSL}
      - Smtp__Host=${SMTP_HOST}
      - Smtp__Port=${SMTP_PORT}
      - Smtp__Username=${SMTP_USERNAME}
      - Smtp__Password=${SMTP_PASSWORD}
      - Smtp__FromEmail=${SMTP_FROM_EMAIL}
      - Smtp__FromName=${SMTP_FROM_NAME}
      - Smtp__SecureSocket=${SMTP_SECURE_SOCKET}
      - Smtp__UseSsl=false
      - Smtp__UseStartTls=true
    ports:
      - "5004:5004"
    depends_on:
      document-db:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5004/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  automation-service:
    container_name: automation-service
    restart: unless-stopped
    build:
      context: ./Automation
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5006
      - ConnectionStrings__DefaultConnection=Host=automation-db;Port=5432;Database=${AUTOMATION_DB_NAME};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - IdentityService__BaseUrl=http://identity-service:5005
      - AppointmentService__BaseUrl=http://appointment-service:5001
      - NotificationService__BaseUrl=http://notification-service:5003
      - Ollama__BaseUrl=${OLLAMA_BASE_URL}
      - Ollama__ModelName=${OLLAMA_MODEL_NAME}
      - Ollama__RequestTimeoutSeconds=${OLLAMA_REQUEST_TIMEOUT_SECONDS}
      - Ollama__NumPredict=${OLLAMA_NUM_PREDICT}
      - Ollama__NumCtx=${OLLAMA_NUM_CTX}
      - Ollama__NumThread=${OLLAMA_NUM_THREAD}
      - Jwt__SecretKey=${JWT_SECRET_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
    ports:
      - "5006:5006"
    depends_on:
      automation-db:
        condition: service_healthy
      <<: *common-service-deps
      appointment-service:
        condition: service_healthy
      ollama:
        condition: service_started
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5006/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend (React + Nginx)
  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    container_name: frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - identity-service
      - appointment-service
      - chat-service
      - notification-service
      - document-service
      - automation-service
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  app-network:
    driver: bridge
    name: appointment-app-network

volumes:
  identity-db-data:
    name: identity-db-data
  appointment-db-data:
    name: appointment-db-data
  chat-db-data:
    name: chat-db-data
  notification-db-data:
    name: notification-db-data
  document-db-data:
    name: document-db-data
  automation-db-data:
    name: automation-db-data
  minio-data:
    name: minio-data
  ollama-data:
    name: ollama-data