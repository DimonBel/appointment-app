# Frontend Environment Configuration Example
# Copy this file to .env and update the values

# Public Base URL for email confirmation links
VITE_PUBLIC_BASE_URL=http://your-frontend-url:8080

# Identity Service API URL (leave empty to use relative path /api/auth)
# For development with separate backend: VITE_IDENTITY_API_URL=http://localhost:5005/api/auth
# For Docker production: leave empty to use nginx proxy (recommended)
VITE_IDENTITY_API_URL=

# MinIO Public URL for file access
VITE_MINIO_PUBLIC_BASE_URL=http://your-minio-url:9000